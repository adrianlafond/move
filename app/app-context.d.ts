import { MoveTimer } from '../move-timer';
export declare const startContext: {
    theme: string;
    timer: MoveTimer;
};
export declare const AppContext: import("preact").Context<{
    theme: string;
    timer: MoveTimer;
}>;
